{
  "name": "ai-desktop-pet",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "matter-js": "^0.20.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^7"
  }
}
